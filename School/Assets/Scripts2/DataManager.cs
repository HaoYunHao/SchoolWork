using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataManager : MonoBehaviour
{
    public const string PlayerDataJSONFileName = "/Player.json";
    public const string PlayerDataXMLFileName = "/Player.xml";
    public const string PlayerDataCSVFileName = "/Player.csv    ";

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
